DATABASE_URL=postgres://booking:booking@localhost:5433/booking
PORT=4000
LOG_LEVEL=info
NODE_ENV=development

# Production only — comma-separated list of allowed CORS origins.
# Example: WEB_ORIGIN=https://my-app.netlify.app
# In development, leave unset and the API will reflect any origin.
# WEB_ORIGIN=
